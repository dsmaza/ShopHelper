<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ShopHelper.Client.Main.MainView"
             Title="{Binding Title}">
    <ScrollView>
        <StackLayout Padding="10">
            <Button Text="Shopping List" Command="{Binding ShowShoppingListCommand}"/>
            <Button Text="Shopping History"/>
            <Button Text="Settings"/>
        </StackLayout>
    </ScrollView>
</ContentPage>